# Text aus der Datei aufgabe05.txt
text = """Python ist eine tolle Programmiersprache.

Sie wird oft in der Schule und in der Industrie verwendet.

Viele Menschen lernen sie gerne."""

# Text in Wörter aufteilen und Leerzeichen entfernen
wörter = text.split()

# Anzahl der Wörter zählen
anzahl_wörter = len(wörter)

# Ergebnis ausgeben
print("Die Gesamtanzahl der Wörter ist:", anzahl_wörter)